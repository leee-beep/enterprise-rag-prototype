# Public browser-side URL for the local Competitor Intelligence API.
# The backend CORS allowlist must include the frontend's browser origin.
NEXT_PUBLIC_COMPETITOR_API_BASE_URL=http://127.0.0.1:8765
