# Hybrid Search

Hybrid search is a fictional test topic in this fixture. It combines two independently computed ranking signals and then merges their candidate lists.

## Test considerations

A future implementation should define deterministic tie-breaking. This file exists to verify recursive discovery of Markdown files in nested folders.